@model IList<string>

@if (Model != null && Model.Count() > 0)
{
    <div>
        <b>
            @for (int i = 0; i < Model.Count() - 1; i++)
            {
              @Model[i] <span> > </span>           
            }
            @Model[Model.Count() - 1]
        </b>
    </div>
}